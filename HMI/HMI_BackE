import java.util.TimerTask;
import java.awt.*;   
import java.awt.event.*;
import java.awt.geom.Line2D;
import javax.swing.*;
import java.awt.Graphics;
import java.util.Timer;
import java.util.TimerTask;

public class HMI_BackE {
    Timer timer1 = new Timer();
    double percent = 0;
    double position = 0;
    
    /*public HMI_BackE() {
        TimerTask task1 = new TimerTask() {
            @Override
            public void run() {
                percent += 0.05;
                position += 0.1;
            }
        };
        timer1.schedule(task1, 1, 1500);
    }*/
    static {
        //System.loadLibrary("HMI_Back");
    }

    public  double getCyclePercent() {
        return percent;
    };

    public  boolean isRideRunning() {
        return false;
    };

    public  boolean isRow1Locked() {
        return true;
    };
    public  boolean isRow2Locked() {
        return true;
    };

    public  boolean isReadyToRun() {
        return isRow1Locked() && isRow2Locked() &&
            //TODO: other conditions for locked rows
            true;
    };

    public  String getErrorMessage() {
        return "NO ERRORS DETECTED";
    }

    public  boolean lockRestraints() {
        return true;
    }
    public  boolean unlockRestraints() {
        return true;
    }

    public  boolean isRotationGondolaClockwise() {
        return false;
    };

    public  boolean isRotationArmClockwise() {
        return true;
    };
    
    public double getPosition() {
        return position;
    }
   
}
